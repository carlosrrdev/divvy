div(
  class="w-full flex flex-col min-h-dvh bg-white"
  x-data="{newModal: false, viewModal: false}"
  )
  include includes/header
  main(class="flex flex-1")
    div(class="flex-1 flex flex-col justify-center items-center container mx-auto p-6")
      div(class="flex flex-col gap-y-2 w-full max-w-48")
        button(
          class="btn py-2 bg-black border-2 border-black text-white"
          @click="newModal = true"
          ) Create divvy
        button(class="btn py-2 border-2 border-black") View saved
  include includes/footer
  include includes/new-divvy-modal